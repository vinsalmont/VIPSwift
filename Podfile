# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VIPSwift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
      pod 'Alamofire', '~> 4.9.1'
      pod 'SwiftyJSON'
      pod 'Kingfisher'
      pod 'OHHTTPStubs/Swift'

  # Pods for VIPSwift

  target 'VIPSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VIPSwiftUITests' do
    # Pods for testing
  end

end
